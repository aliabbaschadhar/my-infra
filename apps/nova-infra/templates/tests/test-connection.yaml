apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nova-infra.fullname" . }}-test-connection"
  labels:
    {{- include "nova-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nova-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
